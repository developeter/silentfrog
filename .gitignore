src/silentfrog/__pycache__/redirect_gui.cpython-312.pyc
tests/__pycache__/
src/silentfrog/__pycache__/__init__.cpython-312.pyc
src/silentfrog/__pycache__/
*.pyc
src/silentfrog/assets/icon1.png
src/silentfrog/assets/icon-old.png
.pytest_cache/
.pytest_runtime_tmp/
.pytest_runtime_cache/
.venv/
build/
src/silentfrog/assets/icon.iconset/

# Local secrets and API credentials. Keep tracked examples only.
.env
.env.*
!.env.example
secrets.local.json
*.secrets.json

# Pinned revision file written by the bootstrap / in-app updater on
# user-mode installs. Dev clones never need this committed.
.silentfrog_revision

# Generated by install_silentfrog.py: real .command launcher placed in
# the install root; the Desktop entry is a symlink to this file.
/Silentfrog.command

# Generated by install_silentfrog.py on macOS: native .app bundle
# living at the install root so the Desktop symlink can target a
# bundle (and the menu bar renders "Silentfrog" instead of "Python").
/Silentfrog.app/

# Claude Code worktrees and session state.
.claude/
